import json
import lzma
from itertools import batched, chain, cycle, repeat
from pathlib import Path
from typing import Any, Callable, cast
from unittest.mock import ANY, MagicMock, create_autospec

import numpy as np
import numpy.typing as npt
import pytest
import torch
from pytest_benchmark.fixture import BenchmarkFixture
from sentence_transformers import SentenceTransformer

from sidecar_search.utils.gpu_utils import imap_multi_gpu

from .. import encode
from ..encode import DocumentEmbeddingBatch, PipelinedEncoder, encode_faster, get_model

EMBEDDING_KEY = "sentence_embedding"  # from SentenceTransformer `.encode`


@pytest.fixture
def encoder_model_factory() -> Callable[[], SentenceTransformer]:
    def model_factory() -> SentenceTransformer:
        return get_model("sentence-transformers/all-MiniLM-L6-v2", False, False)

    _ = model_factory()  # warm-up cache (TODO: can the model be cloned?)
    return model_factory


@pytest.fixture(scope="session")
def golden_sample() -> tuple[list[str], npt.NDArray[np.float16]]:
    local_path = Path(__file__).parent

    with lzma.open(local_path / "data.jsonl.xz") as f:
        documents = [json.loads(line)["document"] for line in f]
    if len(documents) != 1000:
        raise RuntimeError("data did not have expected number of lines")

    # load embeddings generated by SentenceTransformer.encode with an FP16 model
    embeddings = np.load(local_path / "embeddings_fp16.npy")
    if embeddings.shape != (1000, 384) and embeddings.dtype == np.float16:
        raise RuntimeError("embeddings did not have expected dtype/shape")

    return documents, embeddings


def assert_embeddings_close(
    embeddings: npt.ArrayLike, expect_embeddings: npt.ArrayLike
) -> None:
    # Deviation from expected can be as large as +/- 1e-3 due to batching logic
    # inside the SentenceTransformer.encode itself, despite eps being much smaller
    np.testing.assert_allclose(
        np.asarray(embeddings),
        np.asarray(expect_embeddings),
        rtol=1e-3,
        atol=1e-3,
    )


@pytest.mark.parametrize("bf16", (True, False))
def test_get_model(monkeypatch: pytest.MonkeyPatch, bf16: bool) -> None:
    mock_cls: MagicMock = create_autospec(  # duck-typing as MagicMock
        spec=SentenceTransformer
    )
    monkeypatch.setattr(encode, "SentenceTransformer", mock_cls)

    model_name = "asdfgh"
    trust_remote_code = True
    _ = get_model(model_name, bf16, trust_remote_code)

    expected_model_kwargs = {"torch_dtype": torch.bfloat16 if bf16 else torch.float16}
    mock_cls.assert_called_once_with(
        model_name,
        trust_remote_code=trust_remote_code,
        model_kwargs=expected_model_kwargs,
    )


def make_mock_model(
    tokenize_return_value: dict[str, torch.Tensor] | None = None,
    forward_return_value: dict[str, torch.Tensor] | None = None,
) -> MagicMock:
    mock_model = MagicMock(spec=SentenceTransformer)

    mock_model.device = "cpu"
    mock_model.to.return_value = mock_model

    if tokenize_return_value is not None:
        mock_model.tokenize.return_value = tokenize_return_value

    if forward_return_value is not None:
        mock_model.forward.return_value = forward_return_value

    return mock_model


def test_encode_faster() -> None:
    sentences = ["sentence"]
    features = {"key": torch.arange(10).reshape(1, -1)}
    embeddings = {EMBEDDING_KEY: torch.arange(10).reshape(1, -1)}

    mock_model = make_mock_model(features, embeddings)

    results = encode_faster(mock_model, sentences)

    mock_model.tokenize.assert_called_once_with(sentences)
    mock_model.forward.assert_called_once_with(features)
    assert torch.equal(results, embeddings[EMBEDDING_KEY])


@pytest.mark.integration
@pytest.mark.timeout(30, func_only=True)
def test_encode_faster_integration(
    encoder_model_factory: Callable[[], SentenceTransformer],
    golden_sample: tuple[list[str], npt.NDArray[np.float16]],
) -> None:
    n_items = 16
    documents, expect_embeddings = golden_sample
    documents = documents[:n_items]
    expect_embeddings = expect_embeddings[:n_items]
    model = encoder_model_factory()
    embeddings = encode_faster(model, documents)
    assert_embeddings_close(embeddings, expect_embeddings)


@pytest.mark.usefixtures("mock_gpu_env")
class TestPipelinedEncoder:
    def test_tasks_per_gpu_arg_passed(self, monkeypatch: pytest.MonkeyPatch) -> None:
        mock_imap = cast(  # duck-typing as MagicMock
            MagicMock, create_autospec(spec=imap_multi_gpu)
        )
        mock_imap.side_effect = lambda *args, **kwargs: (x for x in iter([]))
        monkeypatch.setattr(encode, "imap_multi_gpu", mock_imap)

        tasks_per_gpu = 10
        encoder = PipelinedEncoder(make_mock_model, tasks_per_gpu=tasks_per_gpu)
        batches = iter([])
        _ = list(encoder.encode(batches))

        mock_imap.assert_called_once_with(ANY, ANY, tasks_per_gpu=tasks_per_gpu)

    def test_round_robin_encode(self) -> None:
        counter = 0
        mock_models: list[MagicMock] = []

        def provision_mock_model() -> Any:
            nonlocal counter
            embedding = torch.tensor([[counter]])
            mock_model = make_mock_model(
                forward_return_value={"sentence_embedding": embedding}
            )
            mock_models.append(mock_model)
            counter += 1
            return mock_model

        n_gpus = torch.cuda.device_count()
        cycles = list(chain(range(n_gpus), range(n_gpus)))

        encoder = PipelinedEncoder(provision_mock_model)
        batches = ([(f"id{i}", f"document{i}")] for i in cycles)

        results = list(encoder.encode(batches))

        for i, mock_model in zip(cycles, cycle(mock_models)):
            mock_model.to.assert_called_with(f"cuda:{i}")
            mock_model.tokenize.assert_called_with([f"document{i}"])

        for i, batch in zip(cycle(range(n_gpus)), results):
            assert batch == [(f"id{i}", torch.tensor([[i]]))]

    def test_empty_iterator(self) -> None:
        encoder = PipelinedEncoder(make_mock_model)
        batches = iter([])
        results = list(encoder.encode(batches))
        assert results == []

    def test_empty_batch(self) -> None:
        encoder = PipelinedEncoder(make_mock_model)
        batches = iter([[]])
        results = list(encoder.encode(batches))
        assert results == [[]]


@pytest.mark.integration
@pytest.mark.timeout(30, func_only=True)
class TestPipelinedEncoderIntegration:
    def test_bulk_encode(
        self,
        encoder_model_factory: Callable[[], SentenceTransformer],
        golden_sample: tuple[list[str], npt.NDArray[np.float16]],
        benchmark: BenchmarkFixture,
    ) -> None:
        documents, expect_embeddings = golden_sample

        encoder = PipelinedEncoder(encoder_model_factory)

        def process() -> list[DocumentEmbeddingBatch]:
            batches_iter = batched(zip(repeat("foo"), iter(documents)), 128)
            batches_iter = encoder.encode(batches_iter)
            return list(batches_iter)

        batches: list[DocumentEmbeddingBatch] = benchmark(process)
        embeddings = torch.vstack([embd for _, embd in chain.from_iterable(batches)])

        assert_embeddings_close(embeddings, expect_embeddings)
